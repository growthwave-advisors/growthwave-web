/**
 * Type Re-exports
 * ================
 * Clean import interface for all type definitions.
 * Usage: import type { BrandId, BrandConfig } from '@types';
 */

export type {
  BrandId,
  DivisionId,
  BrandColors,
  BrandLogos,
  NavItem,
  BrandSEO,
  DivisionLink,
  BrandContact,
  BrandConfig,
  BrandsConfig,
  DivisionLinks,
  ButtonVariant,
  ButtonSize,
} from './brand';
