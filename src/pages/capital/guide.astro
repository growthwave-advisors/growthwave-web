---
/**
 * GrowthWave Capital - Guide Landing Page
 * ========================================
 * Created: February 2026
 * Replaces: previous conference landing page (all event branding removed)
 * 
 * Purpose: Evergreen lead magnet landing page for any channel
 * (LinkedIn, Slack, referrals, email outreach, social media).
 * 
 * Structure: Problem → Solution → Lead Magnet Form → Calendar → Trust
 * 
 * GHL Integration:
 * - Direct POST to GHL via hidden iframe + PDF auto-download
 * - Source tag: source:outreach (generic, works for any channel)
 * - Tags: engagement:lead-magnet-downloaded
 * - Calendar: GHL branded embed (30-min strategy call)
 * 
 * Design System Compliance:
 * - Fixed Island pattern (max-w-7xl content, full-width backgrounds)
 * - Typography stops at lg breakpoint (no xl: or 2xl:)
 * - Royal Purple #7C3AED accent
 * - Responsive padding: px-4 lg:px-8
 */
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';

// Capital color constants
const colors = {
  purple: '#7C3AED',
  purpleLight: '#8B5CF6',
  purpleBg: 'rgba(124,58,237,0.15)',
  purpleBgLight: 'rgba(124,58,237,0.08)',
  ocean: '#265077',
  midnight: '#022140',
  light: '#f0f9ff',
  lightAlt: '#f5faff',
  gray50: '#f9fafb',
  gray100: '#f3f4f6',
  gray200: '#e5e7eb',
  gray400: '#9ca3af',
  gray500: '#6b7280',
  gray600: '#4b5563',
};

// "Before vs After" comparison data
const comparison = [
  {
    before: 'Apply everywhere and hope for the best',
    after: 'Get matched to lenders that fit your profile before you apply',
  },
  {
    before: 'Multiple hard inquiries that tank your score',
    after: 'Soft pull pre-qualification — zero impact to your credit',
  },
  {
    before: 'Weeks of back-and-forth with bank officers',
    after: 'Submit once, get options within 24-48 hours',
  },
  {
    before: 'No idea if you actually qualify until you\'re denied',
    after: 'Understand your strongest options before you apply',
  },
];

// What's inside the guide
const guideContents = [
  {
    icon: 'M3 4h13M3 8h9m-9 4h6m4 0l4-4m0 0l4 4m-4-4v12',
    title: 'Business Credit vs. Personal Credit',
    desc: 'Why separating the two is essential for real estate investors — and how to start building business credit from scratch.',
  },
  {
    icon: 'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z',
    title: 'The 5 Funding Products You Should Know',
    desc: 'Business credit cards, DSCR loans, bridge financing, lines of credit, and term loans — when to use each one.',
  },
  {
    icon: 'M13 10V3L4 14h7v7l9-11h-7z',
    title: 'Speed Strategies',
    desc: 'How to go from application to funded in 15-45 days instead of months. The documentation lenders actually need.',
  },
  {
    icon: 'M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z',
    title: 'Stacking Strategy',
    desc: 'How experienced investors combine multiple funding products to maximize available capital and minimize cost.',
  },
];
---

<BaseLayout
  brand="capital"
  title="Free Business Credit Guide | GrowthWave Capital"
  description="Download the Real Estate Investor's Guide to Business Credit. Learn how to access $50K-$250K+ in business financing without W-2 requirements."
>
  <Header brand="capital" activePage="" />

  <main>
    <!-- ============================================
         HERO SECTION
         - Dark gradient background
         - Badge + Headline + Subhead + CTA
         ============================================ -->
    <section
      class="w-full"
      style={`background: linear-gradient(135deg, ${colors.midnight} 0%, ${colors.ocean} 100%);`}
    >
      <div class="max-w-7xl mx-auto px-4 lg:px-8 py-14 lg:py-20">
        <div class="max-w-3xl mx-auto text-center">
          <!-- Badge -->
          <div
            class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-sm font-semibold mb-5"
            style={`background-color: ${colors.purpleBg}; color: ${colors.purple};`}
          >
            <!-- Book icon -->
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
              <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
            </svg>
            FREE GUIDE &bull; For RE Investors
          </div>

          <!-- Headline -->
          <h1 class="text-3xl lg:text-4xl font-bold text-white leading-tight mb-5">
            The Real Estate Investor&rsquo;s Guide to
            <span style={`color: ${colors.purple};`}> Business Credit</span>
          </h1>

          <!-- Subheadline -->
          <p class="text-base lg:text-lg text-gray-300 leading-relaxed mb-8 max-w-2xl mx-auto">
            Stop leaving money on the table. Learn how investors are accessing $50K&ndash;$250K+
            in business financing&mdash;without W-2 income requirements, without maxing out
            personal credit, and without waiting months for approval.
          </p>

          <!-- CTA -->
          <a
            href="#download"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-white text-sm transition-all hover:scale-[1.02]"
            style={`background-color: ${colors.purple};`}
          >
            Download the Free Guide
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 9l6 6 6-6"></path>
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- ============================================
         PROBLEM / SOLUTION — Before vs After
         - White background
         - Two-column comparison cards
         ============================================ -->
    <section class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            The Funding Gap Most Investors Don&rsquo;t See
          </h2>
          <p class="text-base text-gray-600 max-w-2xl mx-auto">
            You already know how to find deals and run numbers.
            But when it comes to funding, most investors are still doing it the hard way.
          </p>
        </div>

        <!-- Comparison Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 max-w-4xl mx-auto">
          {comparison.map((item) => (
            <div class="flex gap-4 items-start">
              <!-- Before -->
              <div class="flex-1 p-4 rounded-lg border" style={`border-color: ${colors.gray200}; background-color: ${colors.gray50};`}>
                <div class="flex items-center gap-2 mb-2">
                  <span class="text-red-500 text-lg">&times;</span>
                  <span class="text-xs font-semibold text-red-500 uppercase tracking-wide">Before</span>
                </div>
                <p class="text-sm text-gray-600">{item.before}</p>
              </div>
              <!-- After -->
              <div class="flex-1 p-4 rounded-lg border" style={`border-color: rgba(124,58,237,0.2); background-color: ${colors.purpleBgLight};`}>
                <div class="flex items-center gap-2 mb-2">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#7C3AED" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="20 6 9 17 4 12"></polyline>
                  </svg>
                  <span class="text-xs font-semibold uppercase tracking-wide" style={`color: ${colors.purple};`}>After</span>
                </div>
                <p class="text-sm text-gray-700">{item.after}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ============================================
         WHAT'S INSIDE THE GUIDE
         - Light background
         - 4-card grid with icons
         ============================================ -->
    <section
      class="w-full py-14 lg:py-20 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-7xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            What&rsquo;s Inside the Guide
          </h2>
          <p class="text-base text-gray-600 max-w-xl mx-auto">
            A focused playbook you can act on today &mdash; not theory, not fluff.
          </p>
        </div>

        <!-- Content Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-5 max-w-4xl mx-auto">
          {guideContents.map((item) => (
            <div
              class="flex items-start gap-4 p-5 rounded-xl bg-white border"
              style={`border-color: ${colors.gray200}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}
            >
              <!-- Icon -->
              <div
                class="flex-shrink-0 flex items-center justify-center rounded-xl"
                style={`width: 48px; height: 48px; background: linear-gradient(135deg, ${colors.purpleBg} 0%, ${colors.purpleBgLight} 100%); border: 1px solid rgba(124,58,237,0.25);`}
              >
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#7C3AED" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d={item.icon}></path>
                </svg>
              </div>
              <div>
                <h3
                  class="text-base lg:text-lg font-bold mb-1"
                  style={`color: ${colors.midnight};`}
                >
                  {item.title}
                </h3>
                <p class="text-sm text-gray-600 leading-relaxed">{item.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ============================================
         LEAD MAGNET DOWNLOAD FORM
         - White background
         - Centered card with form
         - Direct POST to GHL via hidden iframe + PDF auto-download
         ============================================ -->
    <section id="download" class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-xl mx-auto">
        <!-- Form Card -->
        <div
          class="rounded-2xl p-6 lg:p-8"
          style={`background-color: ${colors.midnight}; box-shadow: 0 20px 40px rgba(2,33,64,0.3);`}
        >
          <!-- Form Header -->
          <div class="text-center mb-6">
            <div
              class="inline-flex items-center gap-2 px-3 py-1 rounded-full text-xs font-semibold mb-4"
              style={`background-color: ${colors.purpleBg}; color: ${colors.purple};`}
            >
              <!-- Download icon -->
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="7 10 12 15 17 10"></polyline>
                <line x1="12" y1="15" x2="12" y2="3"></line>
              </svg>
              FREE DOWNLOAD
            </div>
            <h2 class="text-xl lg:text-2xl font-bold text-white mb-2">
              Get Your Free Guide
            </h2>
            <p class="text-sm text-gray-400">
              Enter your info and the guide is yours &mdash; we&rsquo;ll send it right over.
              No spam, no sales pitch &mdash; just the playbook.
            </p>
          </div>

          <!-- Lead Magnet Form — submits to Netlify Function → GHL API -->
          <form
            id="lead-form"
            class="space-y-3"
          >
            <!-- First Name -->
            <div>
              <input
                type="text"
                name="first_name"
                placeholder="First name"
                required
                class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none focus:ring-2"
                style={`focus:ring-color: ${colors.purple};`}
              />
            </div>

            <!-- Email -->
            <div>
              <input
                type="email"
                name="email"
                placeholder="Email address"
                required
                class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none focus:ring-2"
                style={`focus:ring-color: ${colors.purple};`}
              />
            </div>

            <!-- Phone -->
            <div>
              <input
                type="tel"
                name="phone"
                placeholder="Phone (optional — for your funding consultation)"
                class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none focus:ring-2"
                style={`focus:ring-color: ${colors.purple};`}
              />
            </div>

            <!-- SMS Consent -->
            <label style="display:flex; align-items:flex-start; gap:10px; margin-top:12px; cursor:pointer;">
              <input type="checkbox" name="smsConsent" style="margin-top:3px; flex-shrink:0;" />
              <span style="font-size:13px; color:rgba(255,255,255,0.7); line-height:1.5;">
                I agree to receive SMS messages from <strong style="color:white;">GrowthWave Capital</strong> including appointment reminders, funding updates, and resource delivery. Message frequency varies. Msg &amp; data rates may apply. Reply <strong style="color:white;">STOP</strong> to opt out at any time, <strong style="color:white;">HELP</strong> for help. View our <a href="/privacy" style="text-decoration:underline; color:white;">Privacy Policy</a> and <a href="/terms" style="text-decoration:underline; color:white;">Terms of Service</a>.
              </span>
            </label>

            <!-- Submit Button -->
            <button
              type="submit"
              class="w-full py-3 rounded-lg font-bold text-sm text-white transition-all hover:scale-[1.02] hover:shadow-lg mt-2"
              style={`background-color: ${colors.purple};`}
            >
              Download the Free Guide &rarr;
            </button>

            <!-- Privacy Note -->
            <p class="text-center text-xs text-gray-500 mt-2">
              We respect your privacy. Unsubscribe anytime.
            </p>
          </form>

          <!-- Success State (hidden by default) -->
          <div id="lead-success" class="hidden text-center py-4">
            <div class="text-3xl mb-3">&#x2705;</div>
            <h3 class="text-lg font-bold text-white mb-2">You&rsquo;re In!</h3>
            <p class="text-sm text-gray-300 mb-4">
              Your guide is downloading now. Check your email for a copy and next steps.
            </p>
            <a
              href="/downloads/re-investors-guide-business-credit.pdf"
              download
              class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
              style={`background-color: ${colors.purple};`}
            >
              <!-- Download icon -->
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="7 10 12 15 17 10"></polyline>
                <line x1="12" y1="15" x2="12" y2="3"></line>
              </svg>
              Download PDF Again
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============================================
         WHAT HAPPENS NEXT — Process Steps
         - Off-white background
         - 3-step numbered flow
         ============================================ -->
    <section
      class="w-full py-14 lg:py-20 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-3xl mx-auto text-center">
        <h2
          class="text-2xl lg:text-[32px] font-bold mb-10"
          style={`color: ${colors.midnight};`}
        >
          What Happens After You Download
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <!-- Step 1 -->
          <div class="text-center">
            <div
              class="w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3 text-white font-bold text-sm"
              style={`background-color: ${colors.purple};`}
            >1</div>
            <h3 class="font-semibold text-base mb-2" style={`color: ${colors.midnight};`}>
              Read the Guide
            </h3>
            <p class="text-sm text-gray-600">
              Understand your options and which funding products fit your strategy.
            </p>
          </div>

          <!-- Step 2 -->
          <div class="text-center">
            <div
              class="w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3 text-white font-bold text-sm"
              style={`background-color: ${colors.purple};`}
            >2</div>
            <h3 class="font-semibold text-base mb-2" style={`color: ${colors.midnight};`}>
              Book a Strategy Call
            </h3>
            <p class="text-sm text-gray-600">
              30 minutes with a funding strategist to map your personalized funding roadmap. Free, no obligation.
            </p>
          </div>

          <!-- Step 3 -->
          <div class="text-center">
            <div
              class="w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3 text-white font-bold text-sm"
              style={`background-color: ${colors.purple};`}
            >3</div>
            <h3 class="font-semibold text-base mb-2" style={`color: ${colors.midnight};`}>
              Get Funded
            </h3>
            <p class="text-sm text-gray-600">
              We handle lender matching, documentation, and coordination. You focus on your deals.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- ============================================
         CALENDAR EMBED — Book a Strategy Call
         - White background
         - Branded GHL calendar wrapper
         ============================================ -->
    <section id="calendar" class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-2xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-8">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Ready to Talk Strategy?
          </h2>
          <p class="text-base text-gray-600 max-w-lg mx-auto">
            Book a free 30-minute call. We&rsquo;ll review your situation, identify your
            strongest funding options, and build a plan you can execute.
          </p>
        </div>

        <!-- GHL Calendar Embed -->
        <div
          class="rounded-xl overflow-hidden"
          style={`border: 2px solid rgba(124,58,237,0.3);`}
        >
          <!-- Mini Header -->
          <div class="bg-white px-4 py-3 flex items-center justify-between border-b border-gray-100">
            <div class="flex items-center gap-2">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style={`stroke: ${colors.purple};`}>
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
              <p class="text-sm font-semibold" style={`color: ${colors.midnight};`}>Select a Time</p>
            </div>
            <span
              class="text-xs font-semibold px-2 py-1 rounded-full"
              style={`background-color: ${colors.purpleBgLight}; color: ${colors.purple};`}
            >
              30 min
            </span>
          </div>

          <!-- Calendar iframe — capped height, scrollable -->
          <div class="bg-white calendar-scroll-container" style="height: 420px; overflow-y: auto; -webkit-overflow-scrolling: touch;">
            <iframe
              src="https://crm.advisorhub.io/widget/booking/HHgjKZAFkQknqjPDcsyJ"
              style="width: 100%; border: none;"
              scrolling="no"
              id="guide-calendar-iframe"
              class="ghl-calendar-compact"
              title="Schedule Business Credit Strategy Call"
            ></iframe>
          </div>
        </div>

        <!-- Or link -->
        <p class="text-center text-sm text-gray-500 mt-4">
          Prefer email? Reach out at
          <a href="mailto:info@growthwavecapital.com" class="underline" style={`color: ${colors.purple};`}>
            info@growthwavecapital.com
          </a>
        </p>
      </div>
    </section>

    <!-- ============================================
         WHY GROWTHWAVE — Platform Credibility
         - Off-white background
         - Stats row + platform description + CTAs
         ============================================ -->
    <section
      class="w-full py-14 lg:py-20 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-4xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Why GrowthWave Capital
          </h2>
          <p class="text-base text-gray-600 max-w-2xl mx-auto">
            We combine 15+ years of financial services expertise with a
            funding network that&rsquo;s already delivered results at scale.
          </p>
        </div>

        <!-- Stats Row — 4 cards -->
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-10">
          <div class="p-4 rounded-xl bg-white border text-center" style={`border-color: ${colors.gray200}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>$80M+</p>
            <p class="text-sm text-gray-600 mt-1">Funded Through Our Network</p>
          </div>
          <div class="p-4 rounded-xl bg-white border text-center" style={`border-color: ${colors.gray200}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>7,000+</p>
            <p class="text-sm text-gray-600 mt-1">Clients Funded</p>
          </div>
          <div class="p-4 rounded-xl bg-white border text-center" style={`border-color: ${colors.gray200}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>5,000+</p>
            <p class="text-sm text-gray-600 mt-1">Funding Options</p>
          </div>
          <div class="p-4 rounded-xl bg-white border text-center" style={`border-color: ${colors.gray200}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>15+</p>
            <p class="text-sm text-gray-600 mt-1">Years Financial Services</p>
          </div>
        </div>

        <!-- Platform Description -->
        <div class="max-w-2xl mx-auto space-y-4 text-sm lg:text-base text-gray-600 leading-relaxed">
          <p>
            GrowthWave Capital was built to solve a problem most investors know too well:
            traditional funding wasn&rsquo;t designed for people who generate wealth through
            real estate and business ownership. Banks want W-2 income. Brokers want you
            to wait weeks. Nobody moves at the speed your deals require.
          </p>
          <p>
            Our platform connects you directly to a lender network spanning thousands of
            funding products &mdash; from 0% business credit lines to DSCR loans to bridge
            financing. Soft-pull pre-qualification means you know where you stand before
            a single hard inquiry hits your credit.
          </p>
          <p>
            No upfront fees. No guesswork. Just strategic lender matching backed by a team
            with deep financial services experience and enterprise-grade technology.
          </p>
        </div>

        <!-- CTAs -->
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <a
            href="#download"
            class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
            style={`background-color: ${colors.purple};`}
          >
            Get the Free Guide
          </a>
          <a
            href="/solutions#pre-qualify"
            class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg font-semibold text-sm border-2 transition-all hover:scale-[1.02]"
            style={`color: ${colors.purple}; border-color: ${colors.purple};`}
          >
            See What Options Fit Your Profile &rarr;
          </a>
        </div>
      </div>
    </section>

    <!-- ============================================
         FINAL CTA — Bottom of page
         - Dark gradient background
         ============================================ -->
    <section
      class="w-full py-12 lg:py-16 px-4 lg:px-8"
      style={`background: linear-gradient(135deg, ${colors.midnight} 0%, ${colors.ocean} 100%);`}
    >
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-2xl lg:text-[28px] font-bold text-white mb-3">
          Stop Guessing. Start Getting Funded.
        </h2>
        <p class="text-base text-gray-300 mb-6 max-w-xl mx-auto">
          The guide is free. The strategy call is free. The only cost is waiting while
          your next deal slips away.
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          <a
            href="#download"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
            style={`background-color: ${colors.purple};`}
          >
            Download the Guide
          </a>
          <a
            href="#calendar"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-sm text-white border-2 border-white/30 transition-all hover:bg-white/10"
          >
            Book a Call Instead
          </a>
        </div>
      </div>
    </section>

    <!-- ============================================
         LEGAL DISCLAIMER
         ============================================ -->
    <section class="w-full py-6 px-4 lg:px-8 bg-white">
      <div class="max-w-4xl mx-auto">
        <p class="text-xs text-gray-400 text-center leading-relaxed">
          GrowthWave Capital provides business financing consulting and lender placement services.
          We are not a direct lender.
          Financing subject to lender approval and qualification. No guarantee of approval, specific amounts, or exact terms.
          Results vary based on individual credit profile, business history, and lender requirements.
        </p>
      </div>
    </section>

  </main>

  <Footer brand="capital" />
</BaseLayout>

<!-- Calendar + Form embed styles -->
<style>
  /* Calendar iframe height control */
  .calendar-scroll-container iframe {
    min-height: 600px;
  }

  /* Form input focus ring */
  #lead-form input:focus {
    ring-color: #7C3AED;
    box-shadow: 0 0 0 2px rgba(124, 58, 237, 0.4);
    border-color: #7C3AED;
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }
</style>

<!-- Form submission handler + smooth scroll -->
<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const leadForm = document.getElementById("lead-form");
    const leadSuccess = document.getElementById("lead-success");

    if (leadForm) {
      leadForm.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(leadForm);
        const submitBtn = leadForm.querySelector('button[type="submit"]');
        const originalText = submitBtn.textContent;
        submitBtn.textContent = "DOWNLOADING...";
        submitBtn.disabled = true;
        submitBtn.style.opacity = "0.7";

        try {
          const res = await fetch("/.netlify/functions/ghl-submit", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              formType: "guide",
              firstName: formData.get("first_name") || "",
              email: formData.get("email") || "",
              phone: formData.get("phone") || "",
            }),
          });

          const result = await res.json();

          if (res.ok && result.success) {
            // Trigger PDF download
            const downloadLink = document.createElement("a");
            downloadLink.href = "/downloads/re-investors-guide-business-credit.pdf";
            downloadLink.download = "RE-Investors-Guide-Business-Credit.pdf";
            document.body.appendChild(downloadLink);
            downloadLink.click();
            document.body.removeChild(downloadLink);

            // Show success state
            leadForm.classList.add("hidden");
            leadSuccess.classList.remove("hidden");
          } else {
            console.error("GHL submit error:", result);
            submitBtn.textContent = originalText;
            submitBtn.disabled = false;
            submitBtn.style.opacity = "1";
            alert("Something went wrong. Please try again.");
          }
        } catch (err) {
          console.error("Form submission failed:", err);
          submitBtn.textContent = originalText;
          submitBtn.disabled = false;
          submitBtn.style.opacity = "1";
          alert("Connection error. Please check your internet and try again.");
        }
      });
    }

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", (e) => {
        const targetId = anchor.getAttribute("href");
        if (targetId && targetId !== "#") {
          const target = document.querySelector(targetId);
          if (target) {
            e.preventDefault();
            target.scrollIntoView({ behavior: "smooth", block: "start" });
          }
        }
      });
    });
  });
</script>

<!-- GHL form embed script -->
<script src="https://crm.advisorhub.io/js/form_embed.js" is:inline></script>
