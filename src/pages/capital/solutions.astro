---
/**
 * GrowthWave Capital - Financing Solutions Page
 * ==============================================
 * Created: February 2026
 * Consolidates: investor-funding.astro (deprecated)
 * 
 * Purpose: Product catalog + conversion page. Educates on products,
 * then converts via pre-qualification form or calendar booking.
 * 
 * Flow: Products → Eligibility → Pre-Qualify → Process → Calendar → Trust → CTA
 * 
 * GHL Integration:
 * - Pre-Qual Form: Dual Netlify + GHL submission (formId: uftEVszVPv6XT7Woa7MF)
 * - Calendar: GHL branded embed (30-min strategy call)
 * - Tags: brand:capital, source:website-organic
 * 
 * Design System Compliance:
 * - Fixed Island pattern (max-w-7xl content, full-width backgrounds)
 * - Typography stops at lg breakpoint (no xl: or 2xl:)
 * - Royal Purple #7C3AED accent
 * - Responsive padding: px-4 lg:px-8
 */
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';

// Capital color constants
const colors = {
  purple: '#7C3AED',
  purpleLight: '#8B5CF6',
  purpleBg: 'rgba(124,58,237,0.15)',
  purpleBgLight: 'rgba(124,58,237,0.08)',
  ocean: '#265077',
  midnight: '#022140',
  light: '#f0f9ff',
  lightAlt: '#f5faff',
  gray50: '#f9fafb',
  gray100: '#f3f4f6',
  gray200: '#e5e7eb',
  gray400: '#9ca3af',
  gray500: '#6b7280',
  gray600: '#4b5563',
  creditGreen: '#10B981',
  propertiesCoral: '#FF6B4A',
};

// 5 Financing Products
const products = [
  {
    icon: 'M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z',
    name: 'Business Credit Cards',
    range: '$5K–$50K+',
    timeline: '7–14 days',
    bestFor: 'Rehab costs, gap funding, recurring expenses',
    details: 'Access 0% introductory APR cards designed for business use. Stack multiple cards to build significant available credit. No revenue requirements on many products.',
    requirements: '680+ personal credit score, registered business entity (LLC/Corp), EIN',
    featured: false,
  },
  {
    icon: 'M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z',
    name: 'Business Lines of Credit',
    range: '$10K–$250K',
    timeline: '14–30 days',
    bestFor: 'Working capital, bridge between deals, operating expenses',
    details: 'Revolving credit you draw from as needed. Only pay interest on what you use. Reusable as you pay down balances — no need to reapply.',
    requirements: '650+ credit, 6+ months in business, some revenue history preferred',
    featured: false,
  },
  {
    icon: 'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6',
    name: 'DSCR Loans',
    range: '$75K–$5M+',
    timeline: '21–45 days',
    bestFor: 'Investment property acquisition, portfolio expansion, refinancing',
    details: 'Qualify based on the property\'s income — not yours. No tax returns, no W-2s, no personal income verification. The property\'s debt service coverage ratio determines approval.',
    requirements: '660+ credit, 1.0+ DSCR on subject property, 20–25% down payment',
    featured: true,
  },
  {
    icon: 'M13 10V3L4 14h7v7l9-11h-7z',
    name: 'Bridge Financing',
    range: '$50K–$2M+',
    timeline: '7–14 days',
    bestFor: 'Time-sensitive acquisitions, auction purchases, quick closes',
    details: 'Short-term financing when speed matters more than rate. Close in days, not months. Designed for investors who need to move fast on opportunities.',
    requirements: '620+ credit, clear exit strategy (refinance or sale), property as collateral',
    featured: false,
  },
  {
    icon: 'M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z',
    name: 'Term Loans',
    range: '$25K–$500K',
    timeline: '14–30 days',
    bestFor: 'Equipment, business expansion, large one-time investments',
    details: 'Fixed payments over a set term. Predictable costs for planning. Multiple term lengths and structures available based on use case.',
    requirements: '650+ credit, 12+ months in business, documented revenue',
    featured: false,
  },
];

// FAQ items
const faqItems = [
  {
    q: 'How is this different from going to my bank?',
    a: 'Banks offer their own products. We match you across a network of 5,000+ funding options from hundreds of lenders — finding the best fit for your specific situation instead of forcing you into whatever one bank happens to offer.',
  },
  {
    q: 'Will this hurt my credit score?',
    a: 'No. Pre-qualification uses a soft pull that has zero impact on your credit score. Hard inquiries only happen after you review and approve a specific funding recommendation.',
  },
  {
    q: 'How much can I qualify for?',
    a: 'It depends on your credit profile, business history, and goals. Credit cards typically range $5K–$50K+, lines of credit $10K–$250K, and DSCR loans $75K–$5M+. Pre-qualification gives you realistic numbers before you commit to anything.',
  },
  {
    q: 'Do you guarantee approval?',
    a: 'No. We provide strategic lender matching and consulting — not guarantees. What we do guarantee is that pre-qualification dramatically increases your approval odds by ensuring you only apply where you\'re likely to be approved.',
  },
  {
    q: 'What does this cost?',
    a: 'No upfront fees. We\'re compensated by lenders when your funding closes — similar to how mortgage brokers work. If you don\'t get funded, you don\'t pay.',
  },
  {
    q: 'How long does the process take?',
    a: 'Pre-qualification takes about 3 minutes. From there, timelines depend on the product: business credit cards can fund in 7–14 days, lines of credit in 14–30 days, and DSCR loans in 21–45 days.',
  },
];

// Process steps
const processSteps = [
  {
    num: 1,
    title: 'Pre-Qualify',
    desc: 'Answer a few questions. We run a soft pull (no credit impact) and identify your best options.',
    time: '3 minutes',
  },
  {
    num: 2,
    title: 'Review Your Options',
    desc: 'A funding strategist walks you through your personalized results — products, realistic amounts, and timelines.',
    time: '30 min call',
  },
  {
    num: 3,
    title: 'Choose & Apply',
    desc: 'Select the products that fit your strategy. We handle the applications and lender coordination.',
    time: 'Same day',
  },
  {
    num: 4,
    title: 'Get Funded',
    desc: 'Funds arrive. Deploy your capital into deals, rehab, or operations. Build toward the next round.',
    time: '7–45 days',
  },
];
---

<BaseLayout
  brand="capital"
  title="Financing Solutions | GrowthWave Capital"
  description="Business credit cards, DSCR loans, bridge financing, lines of credit, and term loans. Pre-qualify in 3 minutes with no credit impact."
>
  <Header brand="capital" activePage="Solutions" />

  <main>
    <!-- ============================================
         HERO SECTION
         - Dark gradient background
         - Badge + Headline + Subhead + dual CTAs
         ============================================ -->
    <section
      class="w-full"
      style={`background: linear-gradient(135deg, ${colors.midnight} 0%, ${colors.ocean} 100%);`}
    >
      <div class="max-w-7xl mx-auto px-4 lg:px-8 py-14 lg:py-20">
        <div class="max-w-3xl mx-auto text-center">
          <!-- Badge -->
          <div
            class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-sm font-semibold mb-5"
            style={`background-color: ${colors.purpleBg}; color: ${colors.purple};`}
          >
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
              <polyline points="17 6 23 6 23 12"></polyline>
            </svg>
            5,000+ FUNDING OPTIONS
          </div>

          <!-- Headline -->
          <h1 class="text-3xl lg:text-4xl font-bold text-white leading-tight mb-5">
            Financing Solutions for
            <span style={`color: ${colors.purple};`}>Every Stage of Growth</span>
          </h1>

          <!-- Subheadline -->
          <p class="text-base lg:text-lg text-gray-300 leading-relaxed mb-8 max-w-2xl mx-auto">
            From your first business credit card to a $5M DSCR loan &mdash; we match you
            with the right funding from the right lender. No guesswork, no wasted applications.
          </p>

          <!-- Dual CTAs -->
          <div class="flex flex-wrap justify-center gap-3">
            <a
              href="#pre-qualify"
              class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-white text-sm transition-all hover:scale-[1.02]"
              style={`background-color: ${colors.purple};`}
            >
              Pre-Qualify Now &mdash; No Credit Impact
            </a>
            <a
              href="#products"
              class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-sm text-white border-2 border-white/30 transition-all hover:bg-white/10"
            >
              Explore Products First
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============================================
         PRODUCT CARDS SECTION
         - White background
         - 5 product cards (DSCR featured)
         - Each card links to #pre-qualify
         ============================================ -->
    <section id="products" class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Five Ways to Fund Your Next Move
          </h2>
          <p class="text-base text-gray-600 max-w-2xl mx-auto">
            Each product serves a different purpose. Most investors use a combination &mdash;
            that&rsquo;s where strategic stacking maximizes your available capital.
          </p>
        </div>

        <!-- Product Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 max-w-5xl mx-auto">
          {products.map((product) => (
            <div
              class={`rounded-xl border p-5 relative transition-all hover:shadow-lg ${product.featured ? 'ring-2' : ''}`}
              style={`border-color: ${product.featured ? colors.purple : colors.gray200}; ${product.featured ? `ring-color: ${colors.purple}; box-shadow: 0 4px 20px rgba(124,58,237,0.15);` : ''}`}
            >
              {/* Featured badge */}
              {product.featured && (
                <div
                  class="absolute -top-3 left-4 px-3 py-0.5 rounded-full text-xs font-bold text-white"
                  style={`background-color: ${colors.purple};`}
                >
                  MOST POPULAR
                </div>
              )}

              {/* Icon */}
              <div
                class="flex items-center justify-center rounded-xl mb-4"
                style={`width: 44px; height: 44px; background: linear-gradient(135deg, ${colors.purpleBg} 0%, ${colors.purpleBgLight} 100%); border: 1px solid rgba(124,58,237,0.25);`}
              >
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#7C3AED" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d={product.icon}></path>
                </svg>
              </div>

              {/* Name + Range */}
              <h3
                class="text-base lg:text-lg font-bold mb-1"
                style={`color: ${colors.midnight};`}
              >
                {product.name}
              </h3>
              <p class="text-lg font-bold mb-2" style={`color: ${colors.purple};`}>
                {product.range}
              </p>

              {/* Timeline */}
              <p class="text-xs text-gray-500 mb-3">
                Typical timeline: {product.timeline}
              </p>

              {/* Best For */}
              <div class="mb-3">
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-1">Best For</p>
                <p class="text-sm text-gray-600">{product.bestFor}</p>
              </div>

              {/* Details (expandable via details element) */}
              <details class="mb-4">
                <summary class="text-sm font-semibold cursor-pointer" style={`color: ${colors.purple};`}>
                  How it works &darr;
                </summary>
                <div class="mt-2 space-y-2 text-sm text-gray-600">
                  <p>{product.details}</p>
                  <div class="pt-2 border-t" style={`border-color: ${colors.gray200};`}>
                    <p class="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-1">Requirements</p>
                    <p class="text-sm text-gray-600">{product.requirements}</p>
                  </div>
                </div>
              </details>

              {/* CTA */}
              <a
                href="#pre-qualify"
                class="block w-full py-2.5 rounded-lg font-semibold text-sm text-center transition-all hover:scale-[1.02]"
                style={product.featured
                  ? `background-color: ${colors.purple}; color: white;`
                  : `background-color: ${colors.purpleBgLight}; color: ${colors.purple}; border: 1px solid rgba(124,58,237,0.3);`
                }
              >
                Pre-Qualify for {product.name.split(' ').slice(-1)} &rarr;
              </a>
            </div>
          ))}

          {/* "Not Sure?" card */}
          <div
            class="rounded-xl p-5 flex flex-col justify-center items-center text-center"
            style={`background-color: ${colors.light}; border: 2px dashed ${colors.gray200};`}
          >
            <div class="text-3xl mb-3">&#x1F914;</div>
            <h3
              class="text-base font-bold mb-2"
              style={`color: ${colors.midnight};`}
            >
              Not Sure Which Product?
            </h3>
            <p class="text-sm text-gray-600 mb-4">
              That&rsquo;s exactly what pre-qualification solves. Answer a few questions
              and we&rsquo;ll tell you which products fit your situation.
            </p>
            <a
              href="#pre-qualify"
              class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
              style={`background-color: ${colors.purple};`}
            >
              Find My Best Options &rarr;
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============================================
         DON'T SELF-DISQUALIFY — Callout Section
         - Light background
         - Centered message
         ============================================ -->
    <section
      class="w-full py-10 lg:py-14 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-3xl mx-auto text-center">
        <h2
          class="text-xl lg:text-2xl font-bold mb-3"
          style={`color: ${colors.midnight};`}
        >
          Don&rsquo;t Self-Disqualify
        </h2>
        <p class="text-sm lg:text-base text-gray-600 leading-relaxed max-w-2xl mx-auto">
          Most investors assume they won&rsquo;t qualify &mdash; and never apply.
          The soft-pull pre-qualification takes 3 minutes, costs nothing, and has
          zero impact on your credit. You might be surprised by what&rsquo;s available.
        </p>
      </div>
    </section>

    <!-- ============================================
         PRE-QUALIFICATION FORM
         - White background
         - Dark card with form (matches guide pattern)
         - Dual Netlify + GHL submission
         ============================================ -->
    <section id="pre-qualify" class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-8">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            See What You Qualify For
          </h2>
          <p class="text-base text-gray-600 max-w-lg mx-auto">
            Soft pull only &mdash; zero impact to your credit score.
            Get realistic numbers in minutes, not days.
          </p>
        </div>

        <!-- Form Card -->
        <div
          class="rounded-2xl p-6 lg:p-8"
          style={`background-color: ${colors.midnight}; box-shadow: 0 20px 40px rgba(2,33,64,0.3);`}
        >
          <!-- Badge -->
          <div class="text-center mb-5">
            <div
              class="inline-flex items-center gap-2 px-3 py-1 rounded-full text-xs font-semibold"
              style={`background-color: ${colors.purpleBg}; color: ${colors.purple};`}
            >
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              TAKES 3 MINUTES
            </div>
          </div>

          <!-- Pre-Qual Form -->
          <form
            id="prequal-form"
            data-netlify="true"
            name="capital-prequalify"
            method="POST"
            class="space-y-3"
          >
            <!-- Honeypot -->
            <p class="hidden">
              <label>Leave blank: <input name="bot-field" /></label>
            </p>
            <input type="hidden" name="form-name" value="capital-prequalify" />

            <!-- Name Row -->
            <div class="grid grid-cols-2 gap-3">
              <input
                type="text"
                name="first_name"
                placeholder="First name"
                required
                class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none"
              />
              <input
                type="text"
                name="last_name"
                placeholder="Last name"
                required
                class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none"
              />
            </div>

            <!-- Email -->
            <input
              type="email"
              name="email"
              placeholder="Email address"
              required
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none"
            />

            <!-- Phone -->
            <input
              type="tel"
              name="phone"
              placeholder="Phone number"
              required
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-gray-400 text-sm focus:outline-none"
            />

            <!-- Funding Goal -->
            <select
              name="funding_goal"
              required
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-gray-400 text-sm focus:outline-none appearance-none"
              style="background-image: url('data:image/svg+xml;charset=US-ASCII,%3Csvg%20width%3D%2212%22%20height%3D%228%22%20viewBox%3D%220%200%2012%208%22%20fill%3D%22none%22%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%3E%3Cpath%20d%3D%22M1%201L6%206L11%201%22%20stroke%3D%22%239ca3af%22%20stroke-width%3D%222%22%20stroke-linecap%3D%22round%22%2F%3E%3C%2Fsvg%3E'); background-repeat: no-repeat; background-position: right 12px center;"
            >
              <option value="" disabled selected>How much funding do you need?</option>
              <option value="under-50k">Under $50K</option>
              <option value="50k-100k">$50K – $100K</option>
              <option value="100k-250k">$100K – $250K</option>
              <option value="250k-500k">$250K – $500K</option>
              <option value="500k-plus">$500K+</option>
            </select>

            <!-- Credit Score Range -->
            <select
              name="credit_score"
              required
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-gray-400 text-sm focus:outline-none appearance-none"
              style="background-image: url('data:image/svg+xml;charset=US-ASCII,%3Csvg%20width%3D%2212%22%20height%3D%228%22%20viewBox%3D%220%200%2012%208%22%20fill%3D%22none%22%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%3E%3Cpath%20d%3D%22M1%201L6%206L11%201%22%20stroke%3D%22%239ca3af%22%20stroke-width%3D%222%22%20stroke-linecap%3D%22round%22%2F%3E%3C%2Fsvg%3E'); background-repeat: no-repeat; background-position: right 12px center;"
            >
              <option value="" disabled selected>Estimated credit score</option>
              <option value="720-plus">720+</option>
              <option value="680-719">680 – 719</option>
              <option value="650-679">650 – 679</option>
              <option value="620-649">620 – 649</option>
              <option value="below-620">Below 620</option>
              <option value="not-sure">Not sure</option>
            </select>

            <!-- Submit Button -->
            <button
              type="submit"
              class="w-full py-3 rounded-lg font-bold text-sm text-white transition-all hover:scale-[1.02] hover:shadow-lg mt-2"
              style={`background-color: ${colors.purple};`}
            >
              See My Options &rarr;
            </button>

            <!-- Privacy + soft pull note -->
            <p class="text-center text-xs text-gray-500 mt-2">
              Soft pull only. No impact to your credit score. No obligation.
            </p>
          </form>

          <!-- Success State -->
          <div id="prequal-success" class="hidden text-center py-6">
            <div class="text-3xl mb-3">&#x2705;</div>
            <h3 class="text-lg font-bold text-white mb-2">Pre-Qualification Submitted</h3>
            <p class="text-sm text-gray-300 mb-4">
              A funding strategist will reach out within one business day with your
              personalized results. Want to speed things up? Book a call now.
            </p>
            <a
              href="#calendar"
              class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
              style={`background-color: ${colors.purple};`}
            >
              Book a Strategy Call &rarr;
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============================================
         PROCESS — How It Works
         - Light background
         - 4-step horizontal flow
         ============================================ -->
    <section
      class="w-full py-14 lg:py-20 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-4xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            From Pre-Qualification to Funded
          </h2>
          <p class="text-base text-gray-600">
            Four steps. No guesswork.
          </p>
        </div>

        <!-- Steps Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-5">
          {processSteps.map((step) => (
            <div class="text-center">
              <div
                class="w-10 h-10 rounded-full flex items-center justify-center mx-auto mb-3 text-white font-bold text-sm"
                style={`background-color: ${colors.purple};`}
              >
                {step.num}
              </div>
              <h3
                class="font-semibold text-base mb-1"
                style={`color: ${colors.midnight};`}
              >
                {step.title}
              </h3>
              <p class="text-sm text-gray-600 mb-2">{step.desc}</p>
              <span
                class="inline-block text-xs font-semibold px-2 py-0.5 rounded-full"
                style={`background-color: ${colors.purpleBgLight}; color: ${colors.purple};`}
              >
                {step.time}
              </span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ============================================
         CALENDAR EMBED — Book a Strategy Call
         - White background
         - Branded GHL calendar wrapper
         ============================================ -->
    <section id="calendar" class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-2xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-8">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Prefer to Talk First?
          </h2>
          <p class="text-base text-gray-600 max-w-lg mx-auto">
            Book a free 30-minute strategy call. Walk through your situation
            with a funding strategist and get a personalized plan.
          </p>
        </div>

        <!-- GHL Calendar Embed -->
        <div
          class="rounded-xl overflow-hidden"
          style={`border: 2px solid rgba(124,58,237,0.3);`}
        >
          <!-- Mini Header -->
          <div class="bg-white px-4 py-3 flex items-center justify-between border-b border-gray-100">
            <div class="flex items-center gap-2">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style={`stroke: ${colors.purple};`}>
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
              <p class="text-sm font-semibold" style={`color: ${colors.midnight};`}>Select a Time</p>
            </div>
            <span
              class="text-xs font-semibold px-2 py-1 rounded-full"
              style={`background-color: ${colors.purpleBgLight}; color: ${colors.purple};`}
            >
              30 min
            </span>
          </div>

          <!-- Calendar iframe -->
          <div class="bg-white calendar-scroll-container" style="height: 520px; overflow-y: auto; -webkit-overflow-scrolling: touch;">
            <iframe
              src="https://crm.advisorhub.io/widget/booking/aYwui0ZvKfS58TiaQ7z0"
              style="width: 100%; border: none;"
              scrolling="no"
              id="solutions-calendar-iframe"
              class="ghl-calendar-compact"
              title="Schedule Business Credit Strategy Call"
            ></iframe>
          </div>
        </div>

        <!-- Email fallback -->
        <p class="text-center text-sm text-gray-500 mt-4">
          Prefer email? Reach out at
          <a href="mailto:info@growthwavecapital.com" class="underline" style={`color: ${colors.purple};`}>
            info@growthwavecapital.com
          </a>
        </p>
      </div>
    </section>

    <!-- ============================================
         FAQ SECTION
         - Light background
         - Native details/summary accordion
         ============================================ -->
    <section
      class="w-full py-14 lg:py-20 px-4 lg:px-8"
      style={`background-color: ${colors.light};`}
    >
      <div class="max-w-3xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Common Questions
          </h2>
        </div>

        <!-- FAQ Items -->
        <div class="space-y-3">
          {faqItems.map((faq) => (
            <details class="group rounded-xl bg-white border overflow-hidden" style={`border-color: ${colors.gray200};`}>
              <summary
                class="flex items-center justify-between px-5 py-4 cursor-pointer text-sm lg:text-base font-semibold"
                style={`color: ${colors.midnight};`}
              >
                {faq.q}
                <svg
                  class="flex-shrink-0 w-5 h-5 ml-2 transition-transform group-open:rotate-180"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <polyline points="6 9 12 15 18 9"></polyline>
                </svg>
              </summary>
              <div class="px-5 pb-4 text-sm text-gray-600 leading-relaxed">
                {faq.a}
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- ============================================
         WHY GROWTHWAVE — Platform Credibility
         - White background
         - Stats row + platform description
         ============================================ -->
    <section class="w-full py-14 lg:py-20 px-4 lg:px-8 bg-white">
      <div class="max-w-4xl mx-auto">
        <!-- Section Header -->
        <div class="text-center mb-10">
          <h2
            class="text-2xl lg:text-[32px] font-bold mb-3"
            style={`color: ${colors.midnight};`}
          >
            Why GrowthWave Capital
          </h2>
          <p class="text-base text-gray-600 max-w-2xl mx-auto">
            We combine 15+ years of financial services expertise with a
            funding network that&rsquo;s already delivered results at scale.
          </p>
        </div>

        <!-- Stats Row -->
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-10">
          <div class="p-4 rounded-xl border text-center" style={`border-color: ${colors.gray200}; background-color: ${colors.gray50}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>$80M+</p>
            <p class="text-sm text-gray-600 mt-1">Funded Through Our Network</p>
          </div>
          <div class="p-4 rounded-xl border text-center" style={`border-color: ${colors.gray200}; background-color: ${colors.gray50}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>7,000+</p>
            <p class="text-sm text-gray-600 mt-1">Clients Funded</p>
          </div>
          <div class="p-4 rounded-xl border text-center" style={`border-color: ${colors.gray200}; background-color: ${colors.gray50}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>5,000+</p>
            <p class="text-sm text-gray-600 mt-1">Funding Options</p>
          </div>
          <div class="p-4 rounded-xl border text-center" style={`border-color: ${colors.gray200}; background-color: ${colors.gray50}; box-shadow: 0 4px 16px rgba(0,0,0,0.04);`}>
            <p class="text-2xl lg:text-3xl font-bold" style={`color: ${colors.purple};`}>15+</p>
            <p class="text-sm text-gray-600 mt-1">Years Financial Services</p>
          </div>
        </div>

        <!-- Platform Description -->
        <div class="max-w-2xl mx-auto space-y-4 text-sm lg:text-base text-gray-600 leading-relaxed">
          <p>
            Traditional funding wasn&rsquo;t designed for people who generate wealth through
            real estate and business ownership. Banks want W-2 income. Brokers want you
            to wait weeks. Nobody moves at the speed your deals require.
          </p>
          <p>
            Our platform connects you directly to a lender network spanning thousands of
            funding products &mdash; from 0% business credit lines to DSCR loans to bridge
            financing. Soft-pull pre-qualification means you know where you stand before
            a single hard inquiry hits your credit. No upfront fees. No guesswork.
          </p>
        </div>
      </div>
    </section>

    <!-- ============================================
         FINAL CTA — Bottom of page
         - Dark gradient background
         ============================================ -->
    <section
      class="w-full py-12 lg:py-16 px-4 lg:px-8"
      style={`background: linear-gradient(135deg, ${colors.midnight} 0%, ${colors.ocean} 100%);`}
    >
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-2xl lg:text-[28px] font-bold text-white mb-3">
          Ready to See What You Qualify For?
        </h2>
        <p class="text-base text-gray-300 mb-6 max-w-xl mx-auto">
          3 minutes. No credit impact. Real numbers.
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          <a
            href="#pre-qualify"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-sm text-white transition-all hover:scale-[1.02]"
            style={`background-color: ${colors.purple};`}
          >
            Pre-Qualify Now
          </a>
          <a
            href="#calendar"
            class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold text-sm text-white border-2 border-white/30 transition-all hover:bg-white/10"
          >
            Book a Call Instead
          </a>
        </div>
      </div>
    </section>

    <!-- ============================================
         LEGAL DISCLAIMER
         ============================================ -->
    <section class="w-full py-6 px-4 lg:px-8 bg-white">
      <div class="max-w-4xl mx-auto">
        <p class="text-xs text-gray-400 text-center leading-relaxed">
          GrowthWave Capital provides business financing consulting and lender placement services.
          We are not a direct lender. Financing subject to lender approval and qualification.
          No guarantee of approval, specific amounts, or exact terms.
          Results vary based on individual credit profile, business history, and lender requirements.
          Pre-qualification uses a soft credit inquiry with no impact to your credit score.
        </p>
      </div>
    </section>

  </main>

  <Footer brand="capital" />
</BaseLayout>

<!-- Calendar + Form embed styles -->
<style>
  /* Calendar iframe height control */
  .calendar-scroll-container iframe {
    min-height: 600px;
  }

  /* Form input focus ring */
  #prequal-form input:focus,
  #prequal-form select:focus {
    box-shadow: 0 0 0 2px rgba(124, 58, 237, 0.4);
    border-color: #7C3AED;
  }

  /* Select option text color fix */
  #prequal-form select option {
    color: #1f2937;
    background: white;
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Details/summary accordion styling */
  details summary::-webkit-details-marker {
    display: none;
  }
  details summary {
    list-style: none;
  }
</style>

<!-- Form submission handler + smooth scroll -->
<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const prequalForm = document.getElementById("prequal-form");
    const prequalSuccess = document.getElementById("prequal-success");

    if (prequalForm) {
      prequalForm.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(prequalForm);

        // 1. Submit to Netlify Forms (backup)
        try {
          await fetch("/", {
            method: "POST",
            headers: { "Content-Type": "application/x-www-form-urlencoded" },
            body: new URLSearchParams(formData).toString(),
          });
        } catch (err) {
          console.warn("Netlify form error:", err);
        }

        // 2. Submit to GHL (CRM — no-cors)
        try {
          await fetch("https://services.leadconnectorhq.com/funnels/submit", {
            method: "POST",
            mode: "no-cors",
            headers: { "Content-Type": "application/x-www-form-urlencoded" },
            body: new URLSearchParams({
              formId: "uftEVszVPv6XT7Woa7MF",
              location_id: "dJsBnS06sWTWcGSn1Mjp",
              first_name: formData.get("first_name") || "",
              last_name: formData.get("last_name") || "",
              email: formData.get("email") || "",
              phone: formData.get("phone") || "",
              source: "website-solutions",
              tags: "source:website-organic,brand:capital",
            }).toString(),
          });
        } catch (err) {
          console.warn("GHL form error:", err);
        }

        // 3. Show success state
        prequalForm.classList.add("hidden");
        prequalSuccess.classList.remove("hidden");
      });
    }

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", (e) => {
        const targetId = anchor.getAttribute("href");
        if (targetId && targetId !== "#") {
          const target = document.querySelector(targetId);
          if (target) {
            e.preventDefault();
            target.scrollIntoView({ behavior: "smooth", block: "start" });
          }
        }
      });
    });
  });
</script>

<!-- GHL form embed script -->
<script src="https://crm.advisorhub.io/js/form_embed.js" is:inline></script>
